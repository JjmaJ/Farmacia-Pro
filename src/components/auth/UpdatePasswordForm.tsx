// This form is no longer used. Password recovery now happens fully inside ResetPasswordForm.tsx (OTP flow).
// Kept as placeholder to avoid broken imports in old code.
export function UpdatePasswordForm() {
  return null;
}
